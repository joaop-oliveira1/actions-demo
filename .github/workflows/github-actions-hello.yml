name: GitHub Actions Demo
on: 
  push:
    branches:
      - staging
      - main
jobs:
  staging-deploy:
    name: "Staging Deploy"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building my project For Testing...."
      - run: echo "${{ github.ref_name }}"
      
  production-deploy:
    name: "Production Deploy"
    if: github.ref_name == "main"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building my project For Production...."


